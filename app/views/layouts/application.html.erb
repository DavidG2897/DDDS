<!DOCTYPE html>
<html lang="en">
  <head>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta charset="utf-8"/>
    <title><%= content_for?(:title) ? yield(:title) : "DDDS" %></title>
    <meta name="description" content="<%= content_for?(:description) ? yield(:description) : "Danger Detection Device and System" %>">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
	<% if current_user %>
      <body id="page-top">
        <!-- Page Wrapper -->
        <div id="wrapper">
          <%= render 'partials/sidebar' %>
          <div class="d-flex flex-column" id="content-wrapper">
            <!-- Main Content
            -->
            <div id="content">
              <%= render 'partials/topbar' %>
              <%= render 'partials/logout_modal' %>
              <!-- Content of each page goes here goes here
              -->
              <div class="container-fluid">
                <%= yield %>
              </div>
            </div>
            <%= render 'partials/footer' %>
          </div>
        </div>
        <a class="scroll-to-top rounded" href="#page-top">
          <i class="fas fa-angle-up"></i>
        </a>
      </body>
    <% else %>
      <%= yield	   %>
    <% end %>
  </head>
</html>

