<!-- TODOs -->
<!--
	Remove notice/alerts from dedicated view rendering and move them to modal/popup
	Make hover color of topbar buttons not black
	Finish dedicated views (emergencies, contacts, admin devices, profile
	If possible, include sidebar and remove buttons from topbar
	Make sure home map is always rendered from whenever the user comes
-->

<!DOCTYPE html>
<html lang="en">
  <head>
    <!-- BOOTSTRAP -->
    <link href="https://maxcdn.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://maxcdn.bootstrapcdn.com/bootstrap/4.3.1/js/bootstrap.min.js"></script>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta charset="utf-8"/>
    <title><%= content_for?(:title) ? yield(:title) : "DDDS" %></title>
    <meta name="description" content="<%= content_for?(:description) ? yield(:description) : "Danger Detection Device and System" %>">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
	<%= javascript_include_tag get_google_api_req %>
	<% if current_user %>
      <body id="page-top">
        <!-- Page Wrapper -->
        <div id="wrapper">
          <div class="d-flex flex-column" id="content-wrapper">
            <!-- Main Content -->
            <div id="content">
              <%= render 'partials/topbar' %>
              <%= render 'partials/logout_modal' %>
              <!-- Content of each page goes here goes here -->
              <div class="container-fluid">
                <%= yield %>
              </div>
            </div>
            <%= render 'partials/footer' %>
          </div>
        </div>
      </body>
    <% else %>
	  <div class="container-fluid">
		<%= yield %>
	  </div>
    <% end %>
  </head>
</html>

